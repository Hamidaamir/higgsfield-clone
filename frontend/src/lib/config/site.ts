/** The product’s own identity. Provider and model names are never rebranded here. */
export const siteConfig = {
  name: "Forma",
  /** Rendered as the masthead: the name in the display serif with an accent full stop. */
  descriptor: "AI creative studio",
  description:
    "Create and explore images, video and speech in one AI studio.",
} as const;
